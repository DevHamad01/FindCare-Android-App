<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp"
    android:background="@color/colorBackground">

    <ImageView
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:src="@drawable/illustration_success"
        android:background="@drawable/bg_input_rounded"
        android:layout_marginBottom="32dp"
        android:scaleType="centerInside"/>
        <!-- Note: Using launcher icon as placeholder for the illustration in the reference image -->

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Appointment Booked!"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="@color/textColorPrimary"
        android:gravity="center"
        android:layout_marginBottom="16dp"/>

    <TextView
        android:id="@+id/tvSuccessMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Your appointment with Dr. Ema Miller has been successfully confirmed."
        android:textSize="16sp"
        android:textColor="@color/textColorSecondary"
        android:gravity="center"
        android:layout_marginBottom="40dp"
        android:paddingHorizontal="24dp"/>

    <Button
        android:id="@+id/btnHome"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="Back to Home"
        android:textColor="@color/white"
        android:backgroundTint="@color/black"
        android:textAllCaps="false"
        android:textSize="18sp"/>

</LinearLayout>
